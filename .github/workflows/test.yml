on:
  push:
    branches: [master]    
  pull_request:
    branches: [master]    

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install altinn-app-frontend dependencies
        run: |
          echo ${{ github }}
